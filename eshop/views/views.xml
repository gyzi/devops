<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="eshop.list">
      <field name="name">eshop list</field>
      <field name="model">eshop.eshop</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="eshop.action_window">
      <field name="name">eshop window</field>
      <field name="res_model">eshop.eshop</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="eshop.action_server">
      <field name="name">eshop server</field>
      <field name="model_id" ref="model_eshop_eshop"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="eshop" id="eshop.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="eshop.menu_1" parent="eshop.menu_root"/>
    <menuitem name="Menu 2" id="eshop.menu_2" parent="eshop.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="eshop.menu_1_list" parent="eshop.menu_1"
              action="eshop.action_window"/>
    <menuitem name="Server to list" id="eshop" parent="eshop.menu_2"
              action="eshop.action_server"/>
    -->
  </data>
</odoo>